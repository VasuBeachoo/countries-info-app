import styled from "styled-components";

export const CountryCardBox = styled.div``;

const CountryCard = ({ className }) => {
  return <CountryCardBox className={className}></CountryCardBox>;
};

export default CountryCard;
