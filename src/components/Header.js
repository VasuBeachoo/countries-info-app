import styled from "styled-components";

export const HeaderBox = styled.div``;

const Header = ({ className }) => {
  return <HeaderBox className={className}></HeaderBox>;
};

export default Header;
