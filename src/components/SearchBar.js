import styled from "styled-components";

export const SearchBarBox = styled.div``;

const SearchBar = ({ className }) => {
  return <SearchBarBox className={className}></SearchBarBox>;
};

export default SearchBar;
