import styled from "styled-components";

export const ThemeSwitcherBox = styled.div``;

const ThemeSwitcher = ({ className }) => {
  return <ThemeSwitcherBox className={className}></ThemeSwitcherBox>;
};

export default ThemeSwitcher;
